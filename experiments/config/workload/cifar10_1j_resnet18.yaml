name: cifar10_1j_resnet18
dataset: cifar10
results_dir: exp_results

job_defaults:
  num_batches: 100
  warmup_batches: 5
  learning_rate: 0.001
  device: cuda
  use_amp: true

jobs:
  - name: resnet18
    model_name: resnet18
